Found at: 164613
'async function generateThemes(force↺ Reset Themes) {\n  if (forceRegenerate) {\n    ktThemes = null;\n    ktSelectedIdx = null;\n    /* KT localStorage removed: /* KT localStorage removed: localStorage.removeItem(\'meridian_themes_v1\') */ */;\n  }\n  const body = document.getElementById(\'kt-body\');\n  body.innerHTML = `<div class="kt-loading-state"><div class="spinner"></div> Analysing ${articles.length} articles… this takes about 20 seconds.</div>`;\n\n  // Build article context — titles + tags, up to 400 articles\n  const artContext = articles\n    .filter(a => a.title && a.status !== \'title_only\')\n    .slice(0, 400)\n    .map(a => `- ${a.title}${a.topic ? \' [\' + a.topic + \']\' : \'\'}${(a.tags||[]).length ? \' (\' + a.tags.join(\', \') + \')\' : \'\'}`)\n    .join(\'\\n\');\n\n  const prompt = `You are an intelligence analyst. Below are ${articles.length} article titles with topics and tags from a personal news aggregator focused on geopolitics, finance, and international affairs.\n\nAnalyse these and identify exactly 10 dominant intelligence themes. For each theme produce:\n- name: Short punchy theme name (3-6 words)\n- emoji: One relevant emoji\n- keywords: Array of 8-12 keywords for matching articles\n- overvie'
