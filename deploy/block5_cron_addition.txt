# P2-9 — Economist weekly AI pick (option δ)
#
# This is the cron line installed in the VPS root crontab during the
# Block 5 deploy (Session 77, 2 May 2026).
#
# Schedule: Friday 13:15 UTC (= 14:15 / 15:15 Geneva CET/CEST).
#
# NOTE — schedule changed in S78 (5 May 2026):
#   Originally installed at Thursday 13:15 UTC. Post-S77 diagnostic
#   (see post-S77 final addendum in NOTES.md) revealed Economist
#   publishes online ~21:00 UK Thursday (= 20:00 UTC Thursday in BST).
#   Thursday 13:15 UTC fired ~7 hours BEFORE publication, so the
#   7-day rolling window was full of last week's stragglers, not the
#   new issue. Moved to Friday 13:15 UTC — ~17h after publication,
#   while the new issue is still fresh, well clear of next Thursday's
#   overlap. First run on the new schedule: Friday 8 May 2026.
#
# Rationale for time slot:
#   - Clear of 02:30 UTC (enrich_retry.py)
#   - Clear of 03:40 UTC (wake_sync_vps.sh)
#   - Clear of 09:40 UTC (wake_sync_vps.sh)
#   - Clear of 14:30 UTC (enrich_retry_watchdog.py)
#   - Clear of all minute-0 slots (extension_failure_watchdog.py runs hourly at :00)
#   - Friday afternoon Geneva is also a natural review window — fresh
#     Suggested batch lands when Alex tends to glance at the dashboard.
#   - 13:15 chosen specifically: minute 15 keeps the slot clear of
#     hourly :00 watchdog.
#
# Idempotency: script holds an ISO-week gate in kt_meta. Running twice
# in the same ISO week is a no-op. Safe to retry by hand.

15 13 * * 5 /opt/meridian-server/venv/bin/python3 /opt/meridian-server/economist_weekly_pick.py >> /var/log/meridian/economist_weekly.cron.log 2>&1
